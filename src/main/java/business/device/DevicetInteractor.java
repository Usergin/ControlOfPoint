package business.device;

import data.remote.model.request.Authentication;
import data.remote.model.response.UserResponse;
import io.reactivex.Single;

/**
 * Created by oldman on 20.06.17.
 */
public class DevicetInteractor {
//    Single<UserResponse> getUser(Authentication authentication);
}
